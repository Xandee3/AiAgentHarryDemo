<Window x:Class="AIAgentHarryDemo.ContactDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Kontakt"
        Height="420"
        Width="640"
        MinHeight="420"
        MinWidth="640"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,12,12">
                <TextBlock Margin="0,0,0,4" Text="Anrede" />
                <ComboBox x:Name="SalutationComboBox"
                          Width="96"
                          SelectedIndex="0">
                    <ComboBoxItem Content="" />
                    <ComboBoxItem Content="Herr" />
                    <ComboBoxItem Content="Frau" />
                    <ComboBoxItem Content="Divers" />
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,12,12">
                <TextBlock Margin="0,0,0,4" Text="Vorname" />
                <TextBox x:Name="FirstNameTextBox" />
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="0,0,0,12">
                <TextBlock Margin="0,0,0,4" Text="Name" />
                <TextBox x:Name="LastNameTextBox" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,12,12">
                <TextBlock Margin="0,0,0,4" Text="Telefonnummer" />
                <TextBox x:Name="PhoneNumberTextBox" />
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="0,0,0,12">
                <TextBlock Margin="0,0,0,4" Text="E-Mail-Adresse" />
                <TextBox x:Name="EmailAddressTextBox" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Margin="0,0,0,4" Text="Position" />
            <TextBox x:Name="PositionTextBox" />
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Margin="0,0,0,4" Text="Bemerkungen" />
            <TextBox x:Name="RemarksTextBox"
                     AcceptsReturn="True"
                     MinHeight="88"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </StackPanel>

        <StackPanel Grid.Row="4"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="90"
                    Margin="0,0,8,0"
                    Content="OK"
                    IsDefault="True"
                    Click="OkButton_Click" />
            <Button Width="90"
                    Content="Abbrechen"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
